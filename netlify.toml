[build]
  base = "frontend"
  publish = "dist"
  command = "npm ci && npm run build"
  # Ignora builds quando apenas o backend muda
  ignore = "git diff --quiet HEAD^ HEAD ./frontend"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
